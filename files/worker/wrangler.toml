name = "analises-trader-api"
main = "index.js"
compatibility_date = "2024-09-01"

# Variáveis públicas (não-secret) podem ficar aqui.
# As secrets (GITHUB_TOKEN) você configura pelo painel ou via:
#   npx wrangler secret put GITHUB_TOKEN
